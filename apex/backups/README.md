# APEX Backups

This folder stores small rollback packages for the MVP APEX application.

Current rollback package:

```text
f56174-before-launcher-20260522.zip
```

This was exported from cloud APEX application `56174` before replacing the default blank app with the MVP launcher shell on 2026-05-22.

Do not store credentials, session tokens, or local environment files here.
